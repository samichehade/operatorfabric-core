apiVersion: v1
kind: Service
metadata:
  labels:
    app: lfeoperatorfabric
    service: oauth2
  name: keycloak
spec:
  ports:
  - name: "http"
    port: 8080
    targetPort: 8080
  - name: "5000"
    port: 9990
    targetPort: 9990
  selector:
    service: oauth2

