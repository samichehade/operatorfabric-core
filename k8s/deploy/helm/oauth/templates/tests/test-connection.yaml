apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oauth.fullname" . }}-test-connection"
  labels:
{{ include "oauth.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "oauth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
