apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cards-publication.fullname" . }}-test-connection"
  labels:
{{ include "cards-publication.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cards-publication.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
