apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thirds.fullname" . }}-test-connection"
  labels:
{{ include "thirds.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "thirds.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
