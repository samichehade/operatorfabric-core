apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "client-gateway.fullname" . }}-test-connection"
  labels:
{{ include "client-gateway.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "client-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
